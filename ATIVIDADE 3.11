#include <stdio.h>

int main() {
  int numero, i, primo = 1;

  printf("Digite um número inteiro: ");
  scanf("%d", &numero);

  if (numero == 2 || numero == 3 || numero == 5)
    printf("O número é primo");
  else if(numero % 2 == 0 || numero % 3 == 0 || numero % 5 == 0 || numero == 1)
      printf("O número não é primo");

return 0;
}
